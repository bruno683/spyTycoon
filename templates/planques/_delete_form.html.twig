<form method="post" action="{{ path('planques_delete', {'id': planque.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planque.id) }}">
    <button class="btn">Delete</button>
</form>
